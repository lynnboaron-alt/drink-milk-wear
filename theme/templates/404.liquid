<!-- 404 page — on-brand, editorial -->
<div style="
  min-height: calc(80svh - var(--nav-h));
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: var(--gap);
  background: var(--cream);
">

  <p style="font-family:var(--font-body);font-size:0.5625rem;font-weight:700;letter-spacing:0.25em;text-transform:uppercase;color:var(--mid-cream);margin-bottom:1.5rem;">
    404
  </p>

  <h1 style="font-family:var(--font-serif);font-size:clamp(2.5rem,8vw,5rem);font-weight:400;color:var(--forest);line-height:1;margin-bottom:1.25rem;">
    Lost the fit.<br><em style="color:var(--burgundy);">Not the style.</em>
  </h1>

  <p style="font-family:var(--font-body);font-size:0.9375rem;color:var(--mid-cream);max-width:36ch;line-height:1.65;margin-bottom:2.5rem;">
    That page doesn't exist — but the drop is still live.
  </p>

  <a href="/" class="btn btn-burgundy">Back to the Drop</a>

</div>
